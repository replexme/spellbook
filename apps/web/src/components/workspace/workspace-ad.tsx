/*
 * The self-hosted product shows no advertising. The managed service replaces
 * this file with its ad strip (see the managed web Dockerfile); the
 * workspace reserves a row below the editor only when one is enabled.
 */
export const workspaceAdEnabled: boolean = false;

export function WorkspaceAd() {
  return null;
}
